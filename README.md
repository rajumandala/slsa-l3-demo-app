# slsa-l3-demo-app

A minimal Go CLI used as a hands-on lab for reaching SLSA Source Track L3
and SLSA Build Track L3.

Build and run locally:

```
make run
```

See `docs/threat-model.md` for the attack-to-control mapping this repo is
built around.

<!-- simulated tamper: this line was inserted by rewriting history, not by a new commit -->


<!-- trivial legit change -->

<!-- unsigned commit test -->
